{
  "name": "script-daily-expense",
  "version": "1.0.0",
  "description": "Daily Expense (Apps Script) — TS + esbuild + clasp",
  "type": "module",
  "scripts": {
    "lint": "eslint src --ext .ts",
    "build": "node build.mjs",
    "push": "npm run build && clasp push",
    "pull": "clasp pull",
    "watch": "tsc -w",
    "build:gas": "tsc -p tsconfig.gaslocal.json && node -e \"require('fs').writeFileSync('dist-test/package.json','{\\\"type\\\":\\\"commonjs\\\"}')\"",
    "test:gas": "node test/test-runner.cjs"
  },
  "devDependencies": {
    "@types/google-apps-script": "^1.0.81",
    "@typescript-eslint/eslint-plugin": "^8.8.1",
    "@typescript-eslint/parser": "^8.8.1",
    "cpx": "^1.5.0",
    "esbuild": "^0.24.2",
    "eslint": "^9.13.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-prettier": "^5.2.1",
    "gas-local": "^1.3.1",
    "prettier": "^3.3.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
